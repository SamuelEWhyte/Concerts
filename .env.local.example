# Copy this file to .env.local and fill in your Supabase values.
# In Supabase: open your project → Connect (or Settings → API Keys).
# Copy the Project URL and the publishable key (or legacy anon public key).
# Never use the service role / secret key here.

NEXT_PUBLIC_SUPABASE_URL=https://wrtomlwmowavunjrcuja.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Used for email confirmation links (local dev)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
